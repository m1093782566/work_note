authention, for example, list all private network(vlan) of a user
upload has some question(memi encode image file)
list all private network(vlan) of a user
firewall work with router, private network(vlan) connect with router.
network connect with router or router connect with network has some questions(subnet has not defined).
